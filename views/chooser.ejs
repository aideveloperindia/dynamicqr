<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Choose Action - QR Multiplex</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }
        body {
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, Cantarell, sans-serif;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            min-height: 100vh;
            padding: 20px;
            display: flex;
            align-items: center;
            justify-content: center;
        }
        .container {
            max-width: 500px;
            width: 100%;
            background: white;
            border-radius: 20px;
            padding: 40px 30px;
            box-shadow: 0 20px 60px rgba(0,0,0,0.3);
        }
        h1 {
            color: #667eea;
            margin-bottom: 10px;
            font-size: 24px;
            text-align: center;
        }
        .subtitle {
            color: #666;
            margin-bottom: 30px;
            font-size: 14px;
            text-align: center;
        }
        .btn {
            display: block;
            width: 100%;
            padding: 18px 24px;
            margin: 12px 0;
            background: #667eea;
            color: white;
            text-decoration: none;
            border-radius: 12px;
            font-weight: 600;
            text-align: center;
            transition: all 0.3s;
            border: none;
            cursor: pointer;
            font-size: 16px;
        }
        .btn:hover {
            background: #5568d3;
            transform: translateY(-2px);
            box-shadow: 0 4px 12px rgba(102, 126, 234, 0.4);
        }
        .btn.pay { background: #28a745; }
        .btn.pay:hover { background: #218838; }
        .btn.review { background: #ffc107; color: #333; }
        .btn.review:hover { background: #e0a800; }
        .btn.wifi { background: #17a2b8; }
        .btn.wifi:hover { background: #138496; }
        .btn.menu { background: #fd7e14; }
        .btn.menu:hover { background: #e8680e; }
        .btn.coupon { background: #6f42c1; }
        .btn.coupon:hover { background: #5a32a3; }
        .merchant-info {
            background: #f8f9fa;
            padding: 15px;
            border-radius: 8px;
            margin-bottom: 20px;
            text-align: center;
            color: #666;
            font-size: 14px;
        }
        
        /* Mobile Responsive Styles */
        @media (max-width: 768px) {
            body {
                padding: 10px;
            }
            .container {
                padding: 30px 20px;
                border-radius: 15px;
            }
            h1 {
                font-size: 22px;
            }
            .subtitle {
                font-size: 13px;
            }
            .btn {
                padding: 16px 20px;
                font-size: 15px;
                margin: 10px 0;
            }
        }
        
        @media (max-width: 480px) {
            body {
                padding: 5px;
            }
            .container {
                padding: 25px 15px;
                border-radius: 12px;
            }
            h1 {
                font-size: 20px;
            }
            .subtitle {
                font-size: 12px;
            }
            .btn {
                padding: 14px 18px;
                font-size: 14px;
                margin: 8px 0;
            }
            .merchant-info {
                padding: 12px;
                font-size: 13px;
            }
        }
    </style>
</head>
<body>
    <div class="container">
        <h1>Choose Your Action</h1>
        <p class="subtitle">Select what you'd like to do</p>
        
        <% if (merchant) { %>
        <div class="merchant-info">
            Detected: <strong><%= merchant.name %></strong>
        </div>
        <% } %>

        <% if (merchant && merchant.upi) { %>
            <% if (merchant.upi.gpay_intent) { %>
            <a href="<%= merchant.upi.gpay_intent %>" class="btn pay">💳 Pay with Google Pay</a>
            <% } %>
            <% if (merchant.upi.phonepe_intent) { %>
            <a href="<%= merchant.upi.phonepe_intent %>" class="btn pay">💳 Pay with PhonePe</a>
            <% } %>
            <% if (merchant.upi.paytm_intent) { %>
            <a href="<%= merchant.upi.paytm_intent %>" class="btn pay">💳 Pay with Paytm</a>
            <% } %>
        <% } %>

        <% if (merchant && merchant.google_place_id) { %>
        <a href="https://search.google.com/local/writereview?placeid=<%= merchant.google_place_id %>" class="btn review" target="_blank">⭐ Leave a Review</a>
        <% } %>

        <% if (merchant && merchant.wifi && merchant.wifi.ssid) { %>
        <a href="/p/<%= code %>" class="btn wifi">📶 Show Wi-Fi Details</a>
        <% } %>

        <% if (merchant && merchant.menu_url) { %>
        <a href="<%= merchant.menu_url %>" class="btn menu" target="_blank">🍽️ View Menu</a>
        <% } %>

        <% if (merchant && merchant.coupon_url) { %>
        <a href="<%= merchant.coupon_url %>" class="btn coupon" target="_blank">🎫 Get Coupon</a>
        <% } %>

        <a href="/p/<%= code %>" class="btn" style="background: #6c757d;">🔄 View Full Options</a>
    </div>
</body>
</html>



